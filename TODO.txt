-what happens if acompletion throws?

-agentic loop is not showing thinking messages, decide if we should (assistant response.content text | thinking).

-implement basic tool observability in the cli (did it succeed? if not, what happened?)

-decide if we should implement the file watcher.

-review tools descriptions and exactly what they return: example, "cat -n" in edit seems unecessary.

-decide if returning diff string patches from edit is worth.

-add some control over provider (besides --model flag), reasoning level, etc.

-design TUI architecture.

-add support for headless mode (--p and --interactive parameters).
