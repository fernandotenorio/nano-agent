-add more glob tests.

-see if returning diff string from edit is worth.

-review all builtin prompts (system and tools).

-implement the file watcher.

-update env info + system prompt to better explain the workspace/cwd strategy, also add 'Is this a git repo' field.

-review tools descriptions: example, "cat -n in edit" seems unecessary.

-add more/missing agent tests.

-implement plan mode and remaining basic tools.

-add some control over provider (besides --model flag), reasoning level, etc.

-design TUI architecture.

-add support for headless mode (--p and --interactive parameters).
